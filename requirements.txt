# ── 网页抓取 ──────────────────────────────────────────────────
crawl4ai>=0.4.0          # 异步网页抓取，支持 JS 渲染

# ── TTS 引擎（二选一或都装）────────────────────────────────────
edge-tts>=6.1.0          # 微软在线 TTS，轻量，无需 GPU（推荐新手）
# voxcpm                 # 本地高质量 TTS（需 GPU + 8GB+ 显存）
                         # 取消上面注释启用，安装命令：pip install voxcpm

# ── 音频处理 ───────────────────────────────────────────────────
soundfile>=0.12.0        # 读写 wav 文件（VoxCPM2 使用）
numpy>=1.24.0            # 音频数组拼接

# ── 可选：音频格式转换（wav → mp3）────────────────────────────
# pydub>=0.25.0          # 需要系统安装 ffmpeg
